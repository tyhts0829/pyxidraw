[tool:pytest]
# pytest設定ファイル
testpaths = tests
# Ensure repository root is on import path (correct key is `pythonpath`)
pythonpath = .
addopts = --tb=short -v
# 古いテストファイルを除外
norecursedirs = previous_design benchmarks examples
